name: Build Executables

on:
  workflow_dispatch:
    inputs:
      create_release:
        description: "Create a GitHub Release with the built executables"
        required: false
        type: boolean
        default: false
      release_tag:
        description: "Release tag (required if creating release, e.g. v0.1.0)"
        required: false
        type: string

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact-name: linux-x86_64
          - os: macos-latest
            artifact-name: mac-arm64
          - os: windows-latest
            artifact-name: windows-x86_64

    runs-on: ${{ matrix.os }}

    env:
      PYTHONUTF8: "1"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: requirements-dev.txt

      - name: Create venv and install dependencies
        shell: bash
        run: |
          python -m venv venv
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            venv/Scripts/pip install -r requirements-dev.txt
          else
            venv/bin/pip install -r requirements-dev.txt
          fi

      - name: Package executable
        shell: bash
        run: |
          if [[ "$RUNNER_OS" == "Windows" ]]; then
            venv/Scripts/python scripts/package_executable.py
          else
            venv/bin/python scripts/package_executable.py
          fi

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: caverns-of-carl-${{ matrix.artifact-name }}
          path: dist/

  release:
    if: ${{ inputs.create_release }}
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: List artifacts
        run: ls -lh artifacts/

      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${{ inputs.release_tag }}" \
            --repo "${{ github.repository }}" \
            --title "Caverns of Carl ${{ inputs.release_tag }}" \
            --generate-notes \
            artifacts/caverns-of-carl-*
